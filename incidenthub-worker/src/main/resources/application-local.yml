spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/incidenthub
    username: incidenthub
    password: incidenthub

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true